# Module: sample.py

## Functions

```python
import np

def sample_graph(graph, num_samples):
    """
    Randomly sample a set of nodes and return the induced subgraph.

    Parameters:
        graph (networkx.Graph): NetworkX graph.
        num_samples (int): Number of nodes to sample.

    Returns:
        networkx.Graph: Subgraph containing sampled nodes.
    """
